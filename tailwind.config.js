const twConfig = {
  theme: {
    extend: {
      fontFamily: {
        body: ["var(--font-body)"],
        heading: ["var(--font-heading)"],
        accent: ["var(--font-accent)"],
      },
    },
  },
};

export default twConfig;
